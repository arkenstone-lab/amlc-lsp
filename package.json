{
  "name": "tree-sitter-aml",
  "version": "0.3.0",
  "description": "Tree-sitter grammar for Applied Meta Language (AppliedML)",
  "license": "BSD-3-Clause",
  "private": true,
  "scripts": {
    "generate": "tree-sitter generate",
    "test": "sh test/tree-sitter-fixtures.sh"
  },
  "devDependencies": {
    "tree-sitter-cli": "^0.25.10"
  }
}
