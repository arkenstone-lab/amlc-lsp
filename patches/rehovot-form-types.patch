--- a/oct_check.ml
+++ b/oct_check.ml
@@ -175,7 +175,16 @@
       declared
       (expr { env with locals = (name, declared) :: env.locals } body);
     declared
-  | EAction _ | EUse _ ->
+  | EUse _ as value ->
+    begin match Oct_form_check.check_term env.ast 1 1 env.locals value with
+    | Error message -> invalid message
+    | Ok () ->
+      begin match Oct_form_check.infer env.ast 1 1 env.locals value with
+      | Some typ -> typ
+      | None -> invalid "form result type could not be inferred"
+      end
+    end
+  | EAction _ ->
     invalid "direct form expression requires AML compilation"
 
 and binary env op left right =
--- a/oct_form_check.ml
+++ b/oct_form_check.ml
@@ -185,8 +185,11 @@
+let resolve_scope = ref (fun program (_ : [ `Callable | `Constant ]) name -> name, program)
+
 let rec build program trail cache name =
-  if List.exists (String.equal name) trail then
+  let key, program = !resolve_scope program `Callable name in
+  if List.exists (String.equal key) trail then
     Error ("direct form recursion is forbidden = " ^ name)
   else
-    match List.assoc_opt name cache with
+    match List.assoc_opt key cache with
     | Some value ->
       let actual = List.length trail + value.calls in
       if actual <= Form_runtime_limits.call_depth_max then Ok (value, cache)
@@ -231,8 +234,8 @@
     in
     let* body, cache =
       match body with
-      | Expr value -> expr program (name :: trail) cache env value
-      | Block value -> flow program (name :: trail) cache env value
+      | Expr value -> expr program (key :: trail) cache env value
+      | Block value -> flow program (key :: trail) cache env value
     in
     let* () =
       same out body.typ
@@ -255,7 +258,7 @@
       calls = body.calls + 1;
       direct = C_fun.direct fn;
     } in
-    Ok (item, (name, item) :: cache)
+    Ok (item, (key, item) :: cache)
 
 and flow program trail cache env owner =
   let rec unwrap line column = function
--- a/oct_check.ml
+++ b/oct_check.ml
@@ -101,11 +101,13 @@
       | None ->
         begin
           match find_const env name with
-          | Some _ when List.exists (String.equal name) env.active ->
-            invalid ("constant dependency is cyclic name = " ^ name)
-          | Some item when item.c_typ = TVoid ->
-            expr { env with active = name :: env.active } item.c_value
-          | Some item -> item.c_typ
+          | Some item ->
+            let key, ast = !Oct_form_check.resolve_scope env.ast `Constant name in
+            if List.exists (String.equal key) env.active then
+              invalid ("constant dependency is cyclic name = " ^ name);
+            if item.c_typ = TVoid then
+              expr { env with ast; locals = []; active = key :: env.active } item.c_value
+            else item.c_typ
           | None ->
             if List.mem name ["epoch"; "epoch_time"; "value"] then TInt
             else invalid ("variable is undefined name = " ^ name)
@@ -548,8 +550,9 @@
 
 let check_const ast item =
-  let env = { ast; locals = []; ret = TVoid; active = [item.c_name] } in
+  let key, _ = !Oct_form_check.resolve_scope ast `Constant item.c_name in
+  let env = { ast; locals = []; ret = TVoid; active = [key] } in
   let actual = expr env item.c_value in
   if item.c_typ <> TVoid then
     require "constant type differs" item.c_typ actual
 
 let check_func ast fn =
